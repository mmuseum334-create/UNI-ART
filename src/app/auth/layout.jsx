/**
 * @fileoverview Layout específico para la sección de autenticación
 * Excluye Navbar y Footer del layout raíz para mostrar la página completa
 */

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
